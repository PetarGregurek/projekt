@model List<BoardGameReviews.Models.User>

@{
    ViewData["Title"] = "Users";
}

<section class="game-page game-list-page">
    <div class="game-hero mb-4 d-flex justify-content-between align-items-start gap-3 flex-wrap">
        <div>
            <p class="eyebrow mb-2">Entity Index</p>
            <h1 class="mb-0">Users</h1>
        </div>
        <a asp-controller="Game" asp-action="Index" class="btn game-btn-outline">Games</a>
    </div>

    <div class="entity-block game-table-wrap">
        <div class="entity-header">User list (@Model.Count)</div>
        <div class="table-responsive">
            <table class="table game-table align-middle mb-0">
                <thead>
                    <tr>
                        <th>Username</th>
                        <th>Email</th>
                        <th>Country</th>
                        <th>Age</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model)
                    {
                        <tr>
                            <td>@user.Username</td>
                            <td>@user.Email</td>
                            <td>@user.Country</td>
                            <td>@user.Age</td>
                            <td>
                                <a asp-action="Details" asp-route-id="@user.Id" class="btn btn-sm game-btn-primary">Details</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>
