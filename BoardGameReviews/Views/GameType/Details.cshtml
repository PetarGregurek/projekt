@model BoardGameReviews.Models.GameTypeDetailsViewModel

@{
    ViewData["Title"] = "Game Type Details";
}

<section class="game-page game-details-page">
    <div class="game-hero mb-4 d-flex justify-content-between align-items-start gap-3 flex-wrap">
        <div>
            <p class="eyebrow mb-2">Entity Details</p>
            <h1 class="mb-0">@Model.GameType.Name</h1>
        </div>
        <a asp-action="Index" class="btn game-btn-outline">Back to Game Types</a>
    </div>

    <div class="card game-card mb-4">
        <div class="card-header">Game Type</div>
        <div class="card-body">
            <p class="mb-0"><strong>Description:</strong> @Model.GameType.Description</p>
        </div>
    </div>

    <div class="card game-card">
        <div class="card-header">Games by Type</div>
        <div class="card-body">
            @if (Model.Games.Count == 0)
            {
                <p class="mb-0">No games found for this game type.</p>
            }
            else
            {
                <ul class="list-group game-event-list">
                    @foreach (var game in Model.Games)
                    {
                        <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap gap-2">
                            <span>@game.Name</span>
                            <a asp-controller="Game" asp-action="Details" asp-route-id="@game.Id" class="btn btn-sm game-btn-outline">Open Game</a>
                        </li>
                    }
                </ul>
            }
        </div>
    </div>
</section>
